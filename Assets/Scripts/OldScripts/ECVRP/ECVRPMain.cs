using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ECVRPMain : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        ECVRPMgr.inst.Init();
        //GAMgr.inst.Init();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
