using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;

/*
public class TSPMain : MonoBehaviour
{
    public List<TSPIndividual> TSPPop;
    // Start is called before the first frame update
    void Start()
    {
        TSPMgr.inst.Init();
        GAMgr.inst.Init();
        LKHMgr.inst.Init();
    }
    // Update is called once per frame
    void Update()
    {
        //GAMgr.inst.RunGeneration();
        //GAMgr.inst.GenerateGenerationStats();
        //TSPPop = GAMgr.inst.population.Cast<TSPIndividual>().ToList();
    }

    
}
*/